# =============================================================================
# Neo4j Configuration
# =============================================================================
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password-here
NEO4J_DATABASE=neo4j

# =============================================================================
# {API_NAME} API Configuration
# =============================================================================
# Replace {API_NAME} with your actual API name (e.g., JIRA, SALESFORCE, etc.)
{API_NAME}_URL=https://api.{service}.com/v1.0
{API_NAME}_CLIENT_ID=
{API_NAME}_CLIENT_SECRET=
# Optional: If using API key authentication instead of OAuth
# {API_NAME}_API_KEY=

# =============================================================================
# Sync Configuration
# =============================================================================
# Batch size for bulk operations
SYNC_BATCH_SIZE=100

# Maximum retry attempts for failed API requests
SYNC_MAX_RETRIES=3

# Request timeout in seconds
SYNC_REQUEST_TIMEOUT=60

# =============================================================================
# Optional: Date Range Configuration for Transactional Data
# =============================================================================
# Example: Configure how far back to sync work hours, transactions, etc.
# Option 1: Lookback period in days (from today)
SYNC_{ENTITY}_DAYS_BACK=365

# Option 2: Fixed start date for historical backfill (YYYY-MM-DD format)
# SYNC_{ENTITY}_START_DATE=2020-01-01

# Example for multiple entity types:
# SYNC_TRANSACTIONS_DAYS_BACK=730
# SYNC_EVENTS_START_DATE=2021-01-01

# =============================================================================
# Optional: Feature Flags
# =============================================================================
# Enable/disable specific sync features
SYNC_ENABLE_METRICS=true
SYNC_ENABLE_ANALYTICS=true
SYNC_CREATE_INDEXES=true

# =============================================================================
# Optional: Logging Configuration
# =============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(levelname)s - %(message)s
